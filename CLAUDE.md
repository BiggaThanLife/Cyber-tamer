# Digital Explorer: Cyber Tamer (published as `index.html`; currently v25)

Single-file HTML5/CSS/vanilla JS monster-taming RPG. No backend; saves in localStorage (`cybertamer_save`) with JSON export/import. All code sits in one IIFE (`state` is a closure variable). `render()` dispatches on `state.screen`, and `migrateState(s)` sanitizes every load and import.

## Systems

### Core (v20 and earlier)
- Town hub, 4 seeded biomes (mulberry32; same seed = same layout, fresh fog each visit), favorited/recent seeds.
- Turn-based combat with 8-type effectiveness, SPD dodge/flee, Medicine.
- Capture, Wanted Board, breeding, Dex with milestones.
- Training Ground, Job Board, Evolution Crests (cap 3 per partner), Server Farm (5 tiers of pods).
- Data Mine (multi-hit blocks, depth scaling, resumable dives, mine-only gear).
- Custom in-app modals only. Native `confirm`/`prompt`/`alert` are unreliable in webviews.

### Species (v21-v23)
- `EVOLUTION_LINES[id]` is an array of stage forms, any length. Optional stage-1 fields: `roamer:true` or `habitat:[biomeId|'mine']`; `specialMove`/`ultimateMove` overrides (default from `TYPE_MOVES[type]`); `parts:{tail,eyes,pattern}` (0-4) and `shape:{head,body,wing}`.
- 20 species (8 legacy + 12 exclusive).
- Spawn pools: `encounterPool(biome)` (favored/habitat at weight 2 plus all roamers), `minePool()`, `anomalyPool(biome)`.
- Sprites: cached data URLs via `spriteDataUrl`; uncached renderer is `spriteDataUrlRaw`.
- Unknown species are kept as `missingSpeciesId` and restored when the species exists again.

### Lists (v21)
Active partner pinned first on Storage, Jobs, Training, Breeding. `SORT_OPTIONS` gives 11 sorts, saved as `state.listSort`.

### Biomes 4 and 5 (v23)
- Overclocked Foundry (4): `role:'hazard'` heat-vent tiles (11% of open ground). `hazard.typeMult`: Fire immune, Metal/Earth x0.5, Water/Nature x1.5. Vents never drop HP below 1. Boss: Forgemaster (ferrocog).
- The Ghost Server (5): `fadeFog:{radius:2}` via `applyFog()` (gate always visible), boss ghost-signal hint (`bossSignalText`). Boss: Ghost Admin (glitchling).
- Biome fields: `mapSize`, `levelOffset`, `bossLevelOffset`, `mechanic`.
- Defeating boss N unlocks biome N+1; `migrateState` back-fills unlocks.

### Daily Ops (v23)
- Clock: local date via injectable `clockFn`; monotonic (`lastSeenDay`).
- Login streak: 7-day cycle, +25% per completed cycle (max +100%). One missed day forgiven; 4+ days away gives a welcome-back gift. `LOGIN_REWARDS`' bits climb every day of the cycle (100/130/170/220/280/350/450, no version bump yet) — the old table dipped on day 2 and plateaued on day 4, so a growing streak didn't visibly read as a growing reward; item rewards (capture programs/Databite/Medicine/seed/Evo Crest) stay on the same days as before, only the bits column changed.
- 3 quests/day seeded by date, gated by unlocks, never all chores, bonus cache for clearing all 3. `questEvent(kind, n, meta)` is called from real code paths.
- Daily sector: seeded map with one `role:'cache'` tile from its own PRNG. Every 5th claim gives a Crest.

### Items (v24)
- Shop: 5 tabs from `SHOP_TABS`, `SHOP_ITEMS`, `SUPPLY_ITEMS`, `MODULES`.
- Supplies via `useItem`. Exp Capsules call `gainXpCore` (not boosted by XP Amplifier). Stat Drives use `driveBonus` (cap +10, HP +40), folded into `recalcStats`.
- Battle chips: `battleAction('item', id)`.
- Modules: one slot per monster, `m.module = {uid,id,ticksLeft}`; bag is `inventory.modules[]`. `tickModules()` runs from `advanceTick`. Burn only while active partner (Sync Link burns on bench too). `lastStand`/`legacyLock` have `ticks:null`.
- Gear: sold via `sellGear`; equipping needs a spare (`gearSpare`); discoveries in `state.gearSeen`.
- Farm automation (Large Estate): `FARM_AUTOMATION` 20k/35k/100k. `harvestAll`, `plantAllPods`, `runAutopilot` share `collectPod`. State in `state.farm.auto`.

## v25 changes (mobile pass)
- CSS: at `max-width:600px` or coarse pointer, every button in `#screen` has `min-height:40px`; tab rows gap 8px.
- Wilderness map and Data Mine: `render()` toggles class `wild-screen` on `#screen` for `wild` and `mine` screens. It removes the fixed height and scroll box so text never gets cut off. Map tiles 26px (was 30).
- Controls: small d-pad (`.dpad-sm`, 36px) on the left. Wilderness has EXIT on the right (`.wild-exit`). Mine has Disconnect and Abandon stacked on the right (`.mine-side`); the centre 🔌 button was removed (`#mn-disconnect2` gone, `#mn-disconnect` is now the right-hand button).
- Town menu reordered into related pairs: Gate/Mine, Wanted/Daily, Jobs/Farm, Training/Breeding, Play/Rest, Storage/Inventory, Shop/Dex, Stats Guide/System.
- Logs: `logMsg` also appends to `state.battle.log` during a fight; the battle screen shows it in `#battle-log` (auto-scrolls to newest). A 📜 Log button (`#log-open`, in `renderStatusbar`) opens `openLogModal()` on every in-game screen; `logSeq`/`logSeenSeq` drive the unread dot (not saved).
- Help + tutorial: `HELP_SECTIONS` (9 topics) drive the Help hub, which reuses screen id `statsGuide` (`renderStatsGuide`, transient `helpSection`). `TUTORIAL_STEPS`/`showTutorial(step)` is a 7-step modal shown on New Game (and on Continue if `state.tutorialDone` is false); replayable from Help. `tutorialDone` defaults false in `defaultState()` and true in `migrateState()` for older saves. When mechanics change, update the Help text.
- Updates: `BUILD` (version/date/notes) near the end of `index.html` is the single source of truth for the CURRENT release. `tools/sync_version.py` writes it to `version.json`, which the game polls (on load, on tab focus, every 10 min; max once per 5 min) via `checkForUpdate()`; a newer remote version shows a non-blocking banner (Refresh now / Later). After updating, `maybeShowWhatsNew()` shows the notes once (per-browser key `cybertamer_seen_build`). **Release checklist:** (1) unshift the outgoing `BUILD` (as `{version, date, notes}`) onto the front of `CHANGELOG_HISTORY`, (2) set `BUILD` to the new version/date/notes, (3) run `python tools/sync_version.py`, (4) commit `index.html` + `version.json` together, then push. Versions compare numerically, so 25.10 > 25.9.
- Changelog (v25.11): `CHANGELOG_HISTORY` (newest-first array of past `{version, date, notes}`) plus the live `BUILD` are combined by `fullChangelog()` — nothing is duplicated by hand except at release time (see the checklist line above). `renderChangelog()` (screen id `changelog`, reached from System > 🗓 Update Notes) lists every entry with `formatChangelogDate()` turning `'YYYY-MM-DD'` into `'Mon D, YYYY'`.
- Play (minigame) costs Energy/Hunger every try and has a per-monster `playCooldown` (3 tries then a break, decays while exploring); it no longer restores Hunger/Energy.
- Motion/FX (v25.2): one FX section before the BUILD block. Logic emits cosmetic events with `fx({k:...})`; `afterRenderFx()` (called at the end of `render()`) plays them via Web Animations (`animate()` helper returns null when motion is off). Battle cards are `#bt-enemy`/`#bt-player`; HP bars slide via `battleHpMemo` + `.hpslide`; `queueEvolution()` (from `checkEvolution`) feeds the full-screen `playNextEvolution()` overlay; Bits count up in the status bar. Motion preference = `localStorage cybertamer_motion` (full/reduced, defaults to the device's reduce-motion setting; `.motion-reduced` on `<html>` also kills CSS animation). Effects must never block or delay game state, and every locked-input path has a fallback timer. Note: the in-app preview browser does not paint frames (rAF and animations don't tick), so visual timing needs a real browser or phone.
- Capture rework (v25.4): the old single Trap Sphere item is gone, replaced by `CAPTURE_PROGRAMS` (basicCapture/advancedCapture/hunterExe/containmentExe, each with a `mult` on `estimateCatchChance(enemy, programId)`). Inventory keys match the program ids directly (flat, like `primeMeat`). `attemptCapture(programId)` decides success up front, then `playCapture()` plays a CAPTURE.EXE terminal overlay before `resolveCapture()` applies the result (mirrors the old throw-then-resolve pattern). `captureButtonsHtml()` renders one button per owned program with its live odds. `playScanTransition()` adds a short "UNKNOWN ENTITY DETECTED" scan beat before wild/anomaly/wanted/mine-ambush encounters. `migrateState` converts any saved numeric `trapSphere` 1:1 into `basicCapture`. "Storage Box" was renamed "Network Registry" everywhere (display text only — `state.storage`/`renderStorage`/etc. are unchanged internally); Storage now shows a monster count and each card has an Inspect button (`renderInspect`, screen id `inspect`) with exact IV percentages, full move list and everything held.
- Crops (v25.6): ready farm pods no longer wither — the tick-loop transition to `'withered'` in `advanceTickCore` was removed. `witherTicks` (CROPS) and the `'withered'` pod state/handling (`harvestAll`, `farmCounts`, render) are kept only so an already-withered pod from an old save still clears normally; no new ones occur.
- Perfect streak (v25.6): `registerTimingTier(tier)`, called from the three timing minigames' `onTap` (Play/Job/Training) right after `tier` is resolved. `state.perfectStreak` extends on GREAT and resets otherwise; `state.perfectBest` only updates (and logs a 🏆 line) when actually beaten. `mountTimingGame` shows the live streak/best under the track (`#tg-track` id added for the fx anchor) and fires `fx({k:'perfect', streak})`.
- Locked Nodes / Data Sorting puzzle (v25.7): new map role `'locked'`, placed by its own derived PRNG (`useSeed ^ 0x27220a95`) after all other placement, so biome 0-3 base layouts are untouched (same pattern as hazard/daily). Needs the `decrypter` Explorer Tool (`EXPLORER_TOOLS`, `state.tools`, one-time purchase in the Shop's new `tools` tab, `hasTool(id)`) to attempt; without it, stepping on the tile just logs a message. With it, `moveWild` opens `state.puzzle` (screen `'puzzle'`, `renderPuzzle`/`generateSortPuzzle`/`onPuzzleTap`/`finishPuzzle`) — a ball-sort-style puzzle over `PUZZLE_CHUNK_TYPES`, scrambled by replaying random *legal* moves from a solved state (guarantees solvability), with a `moveBudget`. Success marks the node `opened` and pays Bits + a capture-program item (tier by puzzle difficulty); running out of moves leaves the node unopened (retryable) and costs nothing. The success/fail overlay calls `playFx()` directly rather than via the `fx()` queue, since the puzzle screen is torn down before the next `render()` would otherwise drain it.
- Splice Lab (v25.8, was "Breeding Lab"): display text renamed only — internal names (`renderBreeding`, `state.breeding`, `doBreed`, `isBreedingUnlocked`) are untouched. `doBreed()` now plays `playSpliceReveal(a, b, child, finish)` (full-screen parents-converge-and-reveal overlay, same tap-to-skip/tap-to-dismiss pattern as `playNextEvolution`) before handing off straight to the new monster's Inspect screen (`inspectUid = child.uid; state.screen = 'inspect'`) instead of Storage.
- Timing-minigame result screens (v25.9): `mountTimingResult()` (alongside `mountTimingGame()`) — Job Board and Training Ground now render a frozen result (marker locked at the tapped pct, same `#tg-track` id) with a Continue button instead of switching screen inside `onTap`, because that used to tear down the fx anchor before the queued Perfect-streak animation played. Play was already fine (never left its screen) and is untouched. Attempt objects grow a transient `result: {pct, msg}` field once tapped.
- Monster locking (v25.10): `m.locked` (bool, default false, set in `newMonster`/`migrateMonster`). `toggleLock(uid)` flips it, logs, and fires `fx({k:'lockToggle', uid, locked})` (padlock spins/flashes/bursts on the tapped `[data-lock="<uid>"]` icon — present on both the Network Registry card and Inspect screen). `sellMonster()` refuses a locked monster (belt-and-suspenders; `monsterCard()`'s `canSell`/`sellBlockReason` already disable the button). Locking has no effect beyond the Sell button.
- Changelog (v25.11): `CHANGELOG_HISTORY` (newest-first) + live `BUILD`, combined by `fullChangelog()`. `renderChangelog()`, screen id `changelog`, reached from System > 🗓 Update Notes.
- Data Sorting difficulty (v25.13): the original 3 types/capacity 3 with a scrambleMoves+12 budget solved in a handful of moves and felt trivial. `PUZZLE_CAPACITY` is now 4; `puzzleDifficultyFor()` starts at 4 types (was 3), still capped at +2 by biome index (4/5/6 across the 6 biomes — up to 7 containers, checked it still fits at 375px). `generateSortPuzzle()`'s scramble is `16 + typeCount*4` (was `14 + typeCount*3`) with a tighter `+8` move-budget margin (was `+12`). Capture-program reward tiers in `finishPuzzle()` shifted up to match (`>=6` types → containmentExe, `>=5` → hunterExe, else advancedCapture). Also: `renderPuzzle()`'s no-puzzle fallback now goes to `town` when `state.wild` is unset instead of unconditionally `wild` (was a latent crash path, only reachable via a corrupted/hand-edited save, not normal play).
- Data Breach (v25.14): replaces the Data Sorting ball-sort puzzle entirely (that generator had a real bug, not just a difficulty problem — with a single empty buffer container, every legal move from a solved board had to route through it, so scrambling could only ever disturb one container; see the CHANGELOG). All `PUZZLE_*`/`*Puzzle` code, `state.puzzle` and the `'puzzle'` screen are gone; `state.breach` (screen `'breach'`) and `migrateState` drops any leftover `puzzle` field. Same Locked Node trigger and `decrypter` tool gate as before, only the puzzle itself changed. Standard swap-adjacent match-3 on a 6x6 grid (`BREACH_SIZE`): `generateBreachBoard(biomeIndex, x, y)` deals tiles directly (not via reverse-scrambling) with a look-back check against the 2 previous cells in each direction so no match exists at generation, then verifies `breachHasAnyMove()`, retrying (or dropping a biome tier as a last resort) if not. `breachDifficultyFor()` scales board coverage via `typeCount` (4-6), `wallCount`/Firewalls-to-breach (5-10) and `moveLimit` (30-20) together by biome index — deliberately not move-count alone. `breachResolve()` handles matching, gravity and refill (`breachColumnSegments()` splits each column around fixed Firewall cells so gravity/refill respect them as immovable), cascades, Firewall damage (1 hit per orthogonally-adjacent tile cleared), and auto-`breachReshuffle()` if a resolved board has no legal move left. Match-4 creates a `packetRow`/`packetCol` special (clears on being caught in a later match); match-5 creates `exploit` (3x3 clear on trigger); both are placed at the player's swapped-to cell when possible. Reward/tier logic in `finishBreach()` mirrors the old `finishPuzzle()` (Bits + a capture-program item scaled by `types.length`). Phase 1 only — no obstacle variety beyond Firewall, no multi-objective, no L/T-shape specials, no irregular boards; those are follow-ups, not yet built.
- Decrypter icon (v25.15): changed from 🛡 to 🔑 — it collided with both the Data Breach Firewall glyph and the `reinforcedPlate` gear item's icon.
- Story layer, Phase 1a — state + origins + perks + Patch's confirmation scene (WIP, no version bump yet — see below): first slice of the Cyber Tamer story bible (uploaded story doc). Per-biome key/defeated/recycled flags from the doc are deliberately NOT duplicated as new state — `state.biomeKeyFound`, `state.bossDefeated`, `state.recycleCounts` already ARE `outskirts_key`/`warden_defeated`/`recycled_earth` etc. New state (`defaultState()`/`migrateState()`): `handle`, `origin`, `originConfirmed`, `storyIntroDone`, `firstBattleDone`, `port_level`, `translation_level`, `auditor_stage`, `trace`, `trace_floor`, `ending`, `public_port_enabled` — all inert placeholders until later phases wire them up, except `origin`. `migrateState` does NOT grandfather a pre-existing save silently (an earlier pass of this phase did; superseded per explicit direction) — it flags it (`s.needsStoryReset = true`) and leaves `origin`/`originConfirmed`/`storyIntroDone`/`firstBattleDone` at their normal false/null defaults instead. `performStoryReset(oldState)` is a full `defaultState()` EXCEPT `storage`/`activeId` are carried over from the old save in full (monsters only — everything else, Bits/Inventory/unlocked biomes/farm/Dex/etc., resets), with `dex` rebuilt from that preserved roster via the same `dexMarkCaught()` every real catch already goes through (temporarily repoints the closure's `state` at the new object since `dexMarkCaught`/`getDexEntry` read/write `state` directly — safe because the caller always immediately does `state = performStoryReset(...)` itself right after). `showStoryResetNotice(onConfirm)` is a mandatory one-button modal (not yes/no — the reset is happening regardless, the modal's job is making sure the player understands why and what's kept before it does) shown from all three load paths that can hand back a flagged save: `continueGame()`, the title-screen import (`onImportFileChosen`), and the in-game System-menu import (`onSystemImportChosen`) — each calls `performStoryReset` only after the modal is answered, never automatically. `ORIGINS` (4 entries, keyed by id) + `ORIGIN_IDS`; `showOriginPicker(onPicked)` renders the 4 cards as a reusable modal (used by "Not quite" now, will be reused by the Scene 5 intro in Phase 1b so there's exactly one place that draws origin cards). Three of the four PvE perks are wired now (the fourth, Insider's Clean Logs, only makes sense once Trace exists in Phase 2 — its multiplier `INSIDER_TRACE_MULT=0.85` is defined but unused until then): `WITNESS_FARM_BONUS_CHANCE` (20% chance of an extra pod grow-tick per tick, in `advanceTick`'s farm loop), `KEEPER_WANTED_CATCH_BONUS` (+5 points in `estimateCatchChance` when `enemy.isWanted && origin==='keeper'`), `scavengerBitsBoost()` (10% multiplier, deliberately separate from the existing module-driven `bitsBoost()` since the bible scopes it to chests + Job Board only, not battle/boss/breach bits) applied at the chest-open site and the Job Board `onTap` payout. Patch's origin-confirmation scene: since there's no literal scripted "tutorial fight" in this codebase (`TUTORIAL_STEPS` is a pure text modal with no battle in it), the trigger is interpreted as "the first time the player is back in town after their first battle resolves" — `state.firstBattleDone` is set at the three places a battle actually concludes (the faint branch in `enemyTurn`, and both `state.battle=null` sites in `renderBattle`'s controls — the Wanted-offer path and the plain-Continue path), then checked centrally in `render()` (`if(state.screen==='town') maybeShowOriginConfirmation()`) rather than duplicated at each site, since those three paths don't share a common return point. `originConfirmationShown` (transient, unsaved) stops it from re-popping if town re-renders before the player answers. No UI currently SETS `state.origin` in normal play — that's Scene 5 of the intro (Phase 1b) — so this phase has no player-visible entry point yet and intentionally has no version bump; System menu shows a small origin/perk card when one exists, useful for testing via console injection in the meantime.
- Story layer, Phase 1b — the 5-scene cold-open intro, screen id `storyIntro` (WIP, no version bump yet): gives Phase 1a's `origin`/`handle` their first real, player-visible entry point. `startStoryIntro(afterFn)` is the only entry point — sets transient (unsaved) `introStep=0`, `introAfterFn=afterFn`, `state.screen='storyIntro'`, renders; `renderStoryIntro()` dispatches `introStep` (0-4) to five scene renderers (`renderIntroHandle`/`renderIntroOrigin`/`renderIntroBootLine`/`renderIntroJackIn`/`renderIntroStarter`), each a full screen (not a modal, unlike the reused-elsewhere `showOriginPicker`) so it fits the same full-width layout as every other screen. Scene 0 is a text input for `state.handle` (16 char cap) with a "🎲 Suggest a Handle" button drawing from `HANDLE_SUGGESTIONS`; Scene 1 reuses the `ORIGINS`/`ORIGIN_IDS` data (same table Phase 1a built) as inline full-screen cards, setting `state.origin` directly — this is now the ONLY place that sets it in normal play, so `showOriginConfirmationScene()`'s "Not quite" re-pick and the post-first-battle Patch confirmation from Phase 1a are effectively dormant for anyone who goes through the intro (both are still guarded by `!state.originConfirmed`, which the intro now sets in Scene 2, so they simply never fire — left in place rather than removed, since they're still reachable for a pre-1b save someone hand-edited via System-menu console injection); Scene 2 shows the origin's `bootLine` and sets `state.originConfirmed=true`; Scene 3 is a short atmospheric beat ("UNRECOGNIZED ACCESS. LOGGING."), pure flavor, no state change; Scene 4 narrates receiving the starter (`state.storage[0]`, always Sylvyx from `defaultState()`/`performStoryReset()` — no capture minigame, since there's no starter-choice UI to hook into, matching the explicit "reflect the mechanics we already have" direction) and its Continue button calls `finishStoryIntro()`, which sets `state.storyIntroDone=true`, saves, returns to `town`, then invokes `introAfterFn`. All three flagged-save load paths that used to jump straight to `showTutorial(0)`/`maybeShowWhatsNew()` after `performStoryReset` now route through `startStoryIntro(...)` first: `confirmNewGame`'s `proceed()` (`startStoryIntro(()=>showTutorial(0))`), `continueGame()`'s reset callback (same, using the already-captured `pendingTutorial` flag so a pre-story save that had ALREADY finished the mechanical tutorial doesn't see it again after the reset), and both import paths (`onImportFileChosen`/`onSystemImportChosen`, `startStoryIntro(()=>{})` — imported saves never re-trigger the mechanical tutorial, matching their pre-existing behavior). A brand-new game always sees both the intro AND the tutorial, in that order, per the earlier-agreed phase order (story intro first, then the existing mechanical tutorial, never merged into one). No new save fields — `handle`/`origin`/`originConfirmed`/`storyIntroDone` already existed from Phase 1a; this phase only adds the UI that sets them. Version bump still pending — reasonable to ship this alongside Phase 1c/whichever slice is next, not on its own, since a fresh save now goes through 5 extra taps before Sylvyx but nothing mechanical changed.
- Story layer, Phase 2 — Trace + Auditor notices (WIP, no version bump yet; Phase 6/the ending is explicitly out of scope per direct instruction, so nothing here builds toward it): wires up `state.trace`/`trace_floor`/`auditor_stage`, all placeholders since Phase 1a, and finally activates the Insider origin's dormant Clean Logs perk. No new save fields. `raiseTrace(amount, playerTriggered)` adds to `state.trace` (clamped to `[trace_floor, 100]`), applying `INSIDER_TRACE_MULT` only when `playerTriggered` is true — matches the bible's reduced-source list exactly (Data Breaches, recycling, Layer 2 entries), never boss defeats. Numeric weights were marked **OPEN** in the bible, so these are original: boss defeat +15 (`battleAction`'s boss-win branch), Data Breach success +5 (`finishBreach`), Layer 2 entry +8 (`enterBiome`'s `playTransition` callback in Biome Select, once per new Layer 2 map), recycling +2 (`recycleMonster`). No passive decay was invented — the bible only specifies a rising floor and marks Trace-100 behavior "OPEN, recommend no fail state, story flavor only," so Trace only ever moves via these four sources and the floor is a one-way ratchet. `auditor_stage` (0-5) and `trace_floor` advance together off ONE event, `advanceTraceForBiomeClear(i)`, called from `checkBiomeUnlocks()`'s existing `wasLower` branch (i.e. exactly when biome i's three unlock conditions complete) — "leaving biome i" and "entering biome i+1" are the same transition, so `BIOME_TRACE_FLOORS[i]` (`[5,15,25,40,55]`) and `fireAuditorStage(min(5, i+2))` fire together; entering the Ghost Server (i+1 === BIOMES.length-1) still raises the floor to 55 but fires no notice, matching "Quiet: Auditor notices stop." Because `checkBiomeUnlocks()` loops every not-yet-unlocked biome in one call, a save that satisfies multiple biomes' conditions at once (e.g. via console injection, verified in testing) correctly advances through every intermediate stage/floor in that single call, not just the first. The one exception, Stage 1, fires from `resolveCapture()`'s success branch via `fireAuditorStage(1)` directly ("after first capture," not a biome transition) — also logs Patch's one-line reaction ("Automated. Every abandoned site pings home once in a while.") the same tick, per the bible. `fireAuditorStage(stage)` no-ops if `stage <= state.auditor_stage` (idempotent, safe to call speculatively), otherwise logs `📡 Auditor notice: ...` and shows it via the existing `showAlertModal()` helper (reused as-is — no new modal plumbing) with the shared `AUDITOR_REF` ("Ref. 7741-A") constant baked into each stage's text; Stage 5's text is the only one that reads `state.handle`. Verified in-browser via localStorage injection (setting `bossDefeated`/`biomeKeyFound`/`recycleCounts` directly, since driving real boss fights/breaches through the UI for every biome would be impractical to test): Stage 1 fires correctly on a real capture with Patch's line; a single-biome unlock advances exactly one stage/floor; a simultaneous multi-biome unlock (Tundra AND Undergrid AND Foundry all met before one `recycleMonster()` call) correctly walks through stages 4→5 and floor 25→55 in one pass, silently on the Ghost Server leg. Currently surfaced only as a read-only line on the System menu's existing origin card ("Trace: X/100 (floor Y) — Auditor stage Z/5"), shown once `auditor_stage > 0` — no new screen, per "story flavor only, no fail state."
- Story layer, Phase 3 — Rosetta.exe translation, the light version (WIP, no version bump yet): `state.translation_level` (0-5, placeholder since Phase 1a) now rises the moment each biome's Network Access Key is found, via `raiseTranslationForBiomeKey(biomeIndex)` called from `finishBreach()`'s existing key-grant branch (same `if(!state.biomeKeyFound[i])` guard Trace's key-driven biome unlock already uses, so this never duplicates that check) — level `min(5, biomeIndex+1)`, matching the bible's table where the Foundry key caps it at 5 and the Ghost Server key (6th) does nothing further. This is deliberately the LIGHT option, not the bible's full Rosetta Archive (a menu of decoded logs, scrambled-vs-readable world text that resolves letter-by-letter on level-up, re-reading old logs to find the foreign signature) — there is currently no world text anywhere in the codebase gated behind a reading level, so that reader would be new UI with nothing yet to unlock; instead, leveling up just logs a line (`🔤 Rosetta.exe reads deeper — Level N: <flavor text from TRANSLATION_LEVEL_TEXT>`) and shows the current level + flavor line on the System menu's origin card, same spot Trace uses. No new save fields. If a later pass wants the full Archive, `TRANSLATION_LEVEL_TEXT` (5 entries, index 0-4) is the natural seed for it — it's already the bible's own per-level description text, just not yet attached to actual world text or a browsable log list.
- Story layer, Phase 4 — the Port, narrative-only (WIP, no version bump yet): `state.port_level` (0-6, placeholder since Phase 1a) is pure flavor text riding on top of systems that already unlock themselves independently (Shop/Job Board/Training Ground/Splice Lab all gate on their own pre-existing conditions elsewhere in the codebase) — per direct instruction, port_level never gates anything new here, it only narrates Haven filling in, matching the bible's own Feature column text. Level 1 is set once, the moment `finishStoryIntro()` runs (Patch going live — the bible's "Outskirts start" row), logging a flavor line; levels 2-6 rise off the exact same key-grant event Rosetta's `translation_level` uses, in `finishBreach()` (`raisePortForBiomeKey(biomeIndex)`, level `min(6, biomeIndex+2)`, right next to `raiseTranslationForBiomeKey`), so Port and Rosetta always advance together on a new biome's key, matching how the bible's own two tables move in lockstep. Level 6, from the Foundry key, is "a Public Port terminal appears in Haven, reads OFFLINE" — text only, no terminal screen was built; `public_port_enabled` stays false and nothing reads it yet, left for whenever multiplayer exists. No new save fields, no new screen — shown as a third line on the System menu's origin card, same spot Trace/Rosetta use. Verified in-browser: a brand-new game correctly logs and saves `port_level: 1` the moment the intro's last scene completes, before the mechanical tutorial even opens.
- Story layer, Phase 5 — per-biome story text, the last planned phase (WIP, no version bump yet): two independent, purely-flavor hooks, neither touches Trace/Rosetta/Port/the unlock chain. (1) `BIOME_STORY_TEXT[i]` (a neutral "tell" line plus an origin-tagged reaction, straight from Section 9 of the story bible) logs once the first time the player ever steps into biome i's wilderness, via `maybeShowBiomeStoryText(idx)` called from the single `enterBiome()` closure in Biome Select that every entry path (New Map, Enter Seed, Favorites, Daily) already funnels through — guarded by new `state.biomeStorySeen` (bool array, `BIOMES.length`, same shape/migration pattern as `bossDefeated`/`biomeKeyFound`), since nothing else tracks "have I ever entered biome i" (Trace/Rosetta/Port all fire on CLEARING a biome, not entering one, so none of them double as this trigger). Layer 2 re-entries are explicitly excluded (`else maybeShowBiomeStoryText`) since it's a remix of an already-seen biome, not a first encounter. (2) `RECYCLE_FLAVOR_BY_TYPE` — Patch's one-line reaction to recycling each of the first four bosses' types (Earth/Fire/Water/Void), fired from `recycleMonster()` via `maybeShowRecycleFlavor(type)`, keyed by TYPE rather than biome index because `recycleCounts` is itself type-keyed and recycling happens on the Network Registry screen with no "current biome" context — reuses the existing `state.recycleCounts[type] === 1` check (just-incremented-to-one) as the "first ever" signal, so no new state was needed for this half. Metal (the Foundry/Forgemaster type) is deliberately absent from that table: per the bible, Patch stops making asides on Metal and asks a direct question instead (`showFoundryRecycleQuestion()`, Section 6.5) — a small modal in the same style as Phase 1a's origin-confirmation scene, showing `FOUNDRY_RECYCLE_REPLIES[state.origin]` (one line per origin, bible's own reply table) with an identical, unjudging Patch response regardless of which line shows; verified in-browser end to end (biome-entry tell + origin reaction both log correctly; the Foundry question fires and shows the right origin's reply). Phase 6 (the Ghost Server ending) remains intentionally unbuilt — per direct instruction, the story is being left open-ended rather than resolved, so a future multiplayer mode or additional biomes/centers can pick it up later instead of the game shipping a final choice now. This was the last planned phase before the whole story layer (Phases 1a-5) gets pushed together.
- Story reset notice sized up (same push as Phase 5, no version bump yet): `showStoryResetNotice()` now overrides `#modal-box`'s shared inline `max-width:340px`/font-size to 440px/115% for this one notice only, since it's telling every pre-story player about a reset bigger than any update the game has shipped before — restored back to the shared 340px default (explicitly, not cleared to `''`, since 340px only ever existed as `#modal-box`'s own inline style rather than a CSS class — clearing it outright left every OTHER modal afterward rendering unconstrained-width, caught in testing and fixed before commit) the moment its own Continue button is clicked.
- Story intro prelude — the bible's Section 8 world-building scenes (WIP, no version bump yet, same push cadence as the rest of the story layer): the 5-scene Phase 1b intro started directly at the jack-in/handle prompt, skipping the bible's 4 scenes that establish 2086 and why the world is empty before the player identifies themselves — added per direct feedback that the opening should lay out the world more before reaching town. `INTRO_PRELUDE_SCENES` (4 entries: "The Glow"/"The Husks"/"Your Rig"/"One Signal", their exact bible lines, `beat` for the third/fourth scenes' delayed second line) render via a new `renderIntroPrelude(screenEl, controlsEl, sceneIndex)` — full black screen (`screenEl.style.background='#000'`, cleared back to `''` the moment its Continue is tapped — safe to just clear here since `#screen` has no inline style of its own outside the intro, unlike `#modal-box` below), centered text only, no card chrome, reusing the same Continue-button controls row pattern every other intro scene already uses. `introStep` now runs 0-9: 0-3 are the prelude, 4-9 are the jack-in sequence (handle, origin pick, an incoming-message notice, Patch's boot line, jack-in cue, starter beat), with every scene's own `introStep = N` transition updated to match; `startStoryIntro()` is unchanged (still starts at step 0, now the prelude's first scene instead of the handle prompt). Verified in-browser end to end, all 10 scenes in order, zero console errors, `#screen`'s background and `state.handle`/`origin`/`storyIntroDone` all correct on reaching town.
- Patch incoming-message beat (same push as the prelude, no version bump yet): added between picking an origin and hearing Patch's boot line, per direct feedback — a one-beat "⚠ INCOMING MESSAGE / Another user is sending a message." screen with a "Read" button (`renderIntroPatchNotice`, `introStep` 6) rather than cutting straight to Patch's line, so it reads as something arriving rather than just the next screen in a queue. Same tap-through pattern as every other intro scene; `introStep` 7-9 (boot line/jack-in/starter) shifted up by one to make room, no other content changed.
- Intro starter-beat fix (no version bump yet): Scene 9 (`renderIntroStarter`) used to name the specific species via `state.storage[0]` ("Sylvyx is with you now"). That's correct for a brand-new game, but for a reset-flow replay of an EXISTING save `state.storage[0]` is just whatever happened to be first in that player's preserved roster — often not their active partner and not a fresh capture at all — so a returning player was told they were getting Sylvyx and then found a Ripplet in Storage, a real reported mismatch. The line is now generic ("A monster is with you now") so it reads correctly for both cases; no species lookup left in this scene.
- Archive/diary — a new tab on the Dex screen (no version bump yet): a running journal of story beats the player has actually reached, in the order they discovered them, with locked entries never shown at all (no "???" placeholders) per direct instruction. Built to require zero new story-writing and zero new content-side maintenance going forward. `ARCHIVE_ENTRIES` (id → `{tag, title(), unlocked(s), body(s)}`) is a lookup table where every `body()` re-reads text that already exists elsewhere — `ORIGINS`, `BIOME_STORY_TEXT`, `auditorNoticeText()`, `TRANSLATION_LEVEL_TEXT`, `PORT_LEVEL_TEXT`, `RECYCLE_FLAVOR_BY_TYPE`, `FOUNDRY_RECYCLE_REPLIES` — so editing any of those tables later (the whole point of keeping story content data-driven from Phase 1a onward) automatically updates the archive too; nothing here ever needs touching for a text change. `auditorNoticeText(stage, handleOverride)` grew an optional second param so stage 5's entry can be read outside the closure's live `state.handle` context if ever needed (defaults to the old behavior when omitted).
  - `state.archiveLog` (new save field, array of entry ids, in TRUE discovery order — this is a real diary, not a fixed checklist) is the only thing driving what's visible; `renderArchiveTab()` does nothing but map that array through `ARCHIVE_ENTRIES` and print it as "Entry N — title / tag / body".
  - `archiveEntriesToAdd(s)` is the one shared predicate sweep, called from two places: `syncArchiveLog()` (called every `render()`, guarded to skip entirely during `introReplayMode` so replaying with a different origin can never plant a false diary entry) for live play, and once from `migrateState()` so an existing save's already-true progress (Trace stage 3, Rosetta level 2, etc. from before this feature existed) is grandfathered into the diary instead of starting blank — verified in-browser: a save with `auditor_stage:0`/`translation_level:0`/`port_level:1` from earlier testing came back from `migrateState` with `archiveLog:['origin','jackin','starter','port_1']`, matching its real progress exactly.
  - UI: `dexTab` (transient, 'species'/'archive') plus `dexTabToggleHtml()`/`bindDexTabToggle()` shared by both `renderDex()` and the new `renderArchiveTab()` — a simple 2-button row at the top of the Dex screen. Declaration order in `ARCHIVE_ENTRIES` (built via `Object.keys()`) is the fallback ordering used only when several entries unlock in the exact same render pass (e.g. a multi-biome unlock cascade) or during the migration grandfather sweep, where true discovery order was never recorded — everything unlocked in normal play across separate renders gets its real chronological position.
  - Verified in-browser: toggling Species ↔ Archive works both directions with no regressions to the existing Dex filters, zero console errors, entries render with correct tag/title/body and in the right order.
- Trace redesign — pure story dynamic, supersedes part of the original Phase 2 entry above (no version bump yet): per direct instruction, Trace is no longer influenced by anything the player does. `raiseTrace()` and every one of its four call sites (boss defeat, Data Breach solve, Layer 2 entry, recycling) are gone entirely. `state.trace`/`trace_floor` are now set directly, in lockstep, off `TRACE_BY_BIOME_CLEAR = [10, 25, 45, 70, 85]` (index i = value set when biome i clears, same `checkBiomeUnlocks()` event Auditor stages already used) — every player arrives at the same Trace regardless of playstyle. The 6th checkpoint, 100, is NOT part of that array — it's set separately by a new `maybeTriggerGhostServerArrival(biomeIndex)`, called from every wilderness-entry site (`enterBiome()`'s closure in Biome Select, `enterDailySector()`) alongside `maybeShowBiomeStoryText()`, the moment the player actually steps into the Ghost Server (not merely when it unlocks) — idempotent via the existing `stage <= state.auditor_stage` guard already in `fireAuditorStage`, so it's safe to call on every entry, not just the first.
  - Auditor Stage 6 (new): entering the Ghost Server used to be silent per the bible ("Quiet: Auditor notices stop") — per direct instruction this is now flipped to the opposite beat. `auditorNoticeText()` grew a `case 6`: "Your position has been confirmed. Remediation is no longer scheduled — it has begun." `fireAuditorStage()` grew an optional `onDismiss` callback (passed through to `showAlertModal`) specifically so stage 6 can chain a second modal — Patch breaking his usual dry tone: "...they're not just watching anymore. I don't have time to explain — get out. Now. I'll tell you what I can when it's safe." Both lines are also Archive/diary entries (`auditor_6`, new `ghost_arrival`).
  - Insider's perk repurposed: Clean Logs (~15% less Trace from player-triggered sources) had nothing left to reduce once Trace stopped being player-triggered. Replaced with **Systems Knowledge — Explorer Tools cost 15% less** (`INSIDER_TOOL_DISCOUNT = 0.85`, replacing `INSIDER_TRACE_MULT`), applied via a new `explorerToolPrice(def)` helper used both in the Shop's Tools tab display and its actual purchase deduction. `ORIGINS.insider.perkText` updated to match; since the Archive's origin entry reads `perkText` live, no separate content update was needed there.
  - System menu's Trace display bumped from "Auditor stage X/5" to "X/6" to match the new ceiling.
  - Verified in-browser: simulated all 5 biome-clear conditions at once via a single recycle action (checkBiomeUnlocks cascades through every biome in one call) — landed at `trace:85, auditor_stage:5` exactly as the table predicts; entering the Ghost Server then produced `trace:100, auditor_stage:6`, the Stage 6 modal, Patch's chained warning modal, and both new diary entries, in that order; re-entering afterward correctly did not re-fire (idempotent guard confirmed); Insider's Tools tab showed all four prices discounted exactly 15% (500→425, 300→255, 350→298, 900→765); zero console errors throughout.
- Archive unread dot (no version bump yet): a plain red dot on the town's 📖 Dex button, and again on the Archive tab itself once inside Dex, whenever `state.archiveLog` has entries the player hasn't actually opened the Archive tab to see. New save field `state.archiveSeenCount` (a count, not per-id — `archiveLog` is append-only so a simple length comparison is enough) defaults to 0 in `defaultState()`/`migrateState()`, which deliberately means every EXISTING save's already-accumulated entries show as unread the first time this ships — the explicit point, raised directly: some players already passed a trigger point earlier and would otherwise have no way to know something was added to their diary retroactively. `archiveUnreadCount()` = `archiveLog.length - archiveSeenCount`, read by both the town-menu button (`renderTown`'s button list grew an optional per-button `style` field to support the dot's `position:relative`) and `dexTabToggleHtml()`. Cleared only by `renderArchiveTab()` actually running (sets `archiveSeenCount = archiveLog.length` before building its HTML, so the dot is gone in that same render) — opening Dex to the Species tab does NOT clear it, since browsing species isn't the same as having seen the diary update. Verified in-browser: fresh migration on a save with 17 archive entries and no prior `archiveSeenCount` showed the dot on both the town button and the Archive tab; opening the Archive tab set `archiveSeenCount` to 17 and removed the dot from both places in the same click; zero console errors.
- Rosetta.exe origin scene (no version bump yet): confirmed explicitly NOT a progression gate — Rosetta stays flavor-only, full stop, the earlier "rework into a gate" idea was declined for good. This is purely the one-time "why does this program exist" beat: a 4-part modal chain (Patch: "Hey — can you figure out what they've been saying?" → You: "Hold on. I have an idea." → ". . ." → "EXECUTING ROSETTA.EXE" + a summary of its purpose), `ROSETTA_ORIGIN_SCENE` + `showRosettaWritingScene(step)`/`maybeShowRosettaWritingScene()`. Static/tap-through, not an animated typing effect — deliberately, since the in-app preview browser doesn't render animation frames reliably and this needed to be actually verified while building, not just trusted.
  - Triggers on the first biome's boss falling specifically (`state.bossDefeated[0]`), NOT on the Outskirts key being found — the three biome-unlock conditions (boss/recycle/key) can complete in any order, so this is its own flag (`state.rosettaSceneShown`), entirely independent of `raiseTranslationForBiomeKey()`'s Level 1 line, which keeps firing on its own existing trigger. The two sit alongside each other exactly as intended: this is the origin story, the level-up lines are Rosetta getting better over time.
  - Checked centrally in `render()` (`if(state.screen==='town'){ maybeShowOriginConfirmation(); maybeShowRosettaWritingScene(); }`) — the exact same pattern `maybeShowOriginConfirmation` already used, chosen specifically so it also catches players who already had `bossDefeated[0]` true before this shipped: per direct instruction, those players get the scene itself (not just a log line) the next time they're back in town, with zero state reset and zero progress lost — `rosettaSceneShown` defaults to `false` for every pre-existing save via `migrateState`, so it's a pure narrative catch-up. Also logged to the Archive/diary (`rosetta_writing` entry) and picked up by the existing unread-dot system automatically.
  - Verified in-browser end to end on a retroactive save (`bossDefeated[0]:true`, no prior `rosettaSceneShown`): all 4 beats fired correctly on returning to town, `rosettaSceneShown` set true, `translation_level` untouched (confirming no progress was reset), archived, no re-fire on a subsequent town render, zero console errors.
- Replay Intro (System menu, no version bump yet): a "▶ Replay Intro" button, shown once `state.storyIntroDone` (so it can't appear before a first playthrough exists), calls `replayStoryIntro()` — deliberately view-only per direct instruction, not a real redo. It snapshots `handle`/`origin`/`originConfirmed` into `introReplaySnapshot`, sets a transient `introReplayMode` flag, then reuses `startStoryIntro(()=>{})` as-is (same 10 scenes, same code, no new intro logic). The only change is in `finishStoryIntro()`: when `introReplayMode` is set, it restores the three snapshotted fields and skips every one-time side effect (`storyIntroDone`/`port_level` are already set from the real playthrough) instead of doing its normal end-of-intro work. This means the player can genuinely tap through different handle/origin choices while replaying — Patch's boot line, the perk text, everything reflects the choice live — but the moment they reach the end it's all reverted to what they actually have; nothing about their real save changes. Verified in-browser: replayed with a different origin (Keeper) than the save's real one (Witness), confirmed Patch's boot line and perk text reflected Keeper mid-replay, then confirmed the save was back to Witness/the original handle/originConfirmed after finishing, landed cleanly in town, no tutorial re-trigger, zero console errors.
- Gear combat mechanics, 19 new items (v25.31): 6 new mechanics never seen in this game before, each a small Common→Rare→Legendary family (weakest at depth 1, strongest gated the same way Legendary already is — never before depth 25) — direct instruction: "small family with increasing strength where the better ones are more rare... found only in the deeper parts of the mining area." `GEAR_ITEMS` grows from 17 to 36. Crit (`critChance`), life-steal (`lifesteal`), multi-hit (`multiHitChance`, a second strike at half power), counter-attack (`counterChance`) and block (`blockChance`) are plain per-hit rolls, computed fresh via new `gearCritChance`/`gearLifesteal`/`gearMultiHitChance`/`gearCounterChance`/`gearBlockChance` helpers — same additive-stack pattern as the existing `gearDodgeBonus`/`gearFleeBonus`. Status-on-hit (Overheat/Corruption, `statusOnHit:{name,chance,dmgPct,turns}`) is the deliberately-scoped-down sixth mechanic — only Rare+Legendary (no Common tier), and the one genuinely new piece of battle infrastructure: `b.enemy.status` is a real persistent DOT that ticks at the start of `battleAction()`, before the player's action resolves, dealing `dmgPct` of the enemy's max HP per remaining turn. Lag Spike/Power Surge (freeze/shock) were explicitly NOT included this pass — held back until this DOT system is proven out, since they'd touch turn order and dodge, the trickiest parts of combat to get right.
  - `battleAction()`'s and `enemyTurn()`'s old inline enemy-defeat block (bits/XP/boss-unlock logic, ~30 lines) was extracted into a shared `resolveEnemyDefeat(b, p)` — needed because THREE different things can now kill the enemy in the same turn (the player's move, a multi-hit follow-up, or the status DOT ticking down), and a counter-attack can also finish it off from `enemyTurn()`. Each call site (`battleAction`'s post-move check, `battleAction`'s pre-move DOT tick, `enemyTurn`'s counter-attack) calls the same function rather than duplicating the reward/unlock logic three times.
  - Combat order: DOT tick (start of player's turn) → player's move (crit → base hit → multi-hit follow-up → life-steal off total damage → status-on-hit roll, one status at a time, never stacked) → enemy's turn (existing Firewall shield → new block-chance roll → existing dodge roll → damage → new counter-attack roll, only while the player is still standing).
  - Verified in-browser via a `Math.random` override (deterministic proc/fail) across three separate gear loadouts: crit + Overheat (critical hit logged, Overheat applied, ticked for exactly its 3 turns, "wears off" fired on schedule, enemy defeat via `resolveEnemyDefeat` paid out correctly); multi-hit + life-steal (second-strike log line, HP visibly healed by the drain); counter-attack alone (enemy's hit landing, then the counter logged immediately after) and block alone (barrier line, zero damage taken) — each isolated by swapping gear slots between reloads since block and dodge both being force-triggered together would have made counter untestable otherwise. Inventory screen confirmed correct effect text, sell price (◈500 via the existing Legendary tier fallback, no new `GEAR_SELL` entries needed) and equip state for every new item. Zero console errors throughout.
- Data Mine now costs Energy, not Hunger (no version bump yet): `mineAction()`'s per-dig drain and its depletion guard both switched from `p.hunger` to `p.energy` — direct instruction, since digging draining Hunger (a food/rest resource) never made much sense next to Energy existing as the "can this partner keep doing physical activity" resource everywhere else. Running out mid-dive now ejects the player back to town with a popup ("⚠ Out of Energy" — mirrors the existing wilderness "⚠ Out of Hunger" ejection popup's exact pattern, `showAlertModal` + a log line) instead of the old silent "too exhausted to keep digging" refusal that left the player stuck facing a wall with no explanation. Ejecting uses the same non-destructive path as the Mine's own Disconnect button — `state.screen='town'`, `state.mine` untouched — so the dive is paused, not lost. Verified in-browser: an active partner at 0 Energy digging in a live dive was ejected to town, the dive object survived intact (`state.mine` still present, reconnectable), Hunger was confirmed completely untouched by the whole interaction, zero console errors.
- Exp Capsule price rebalance (no version bump yet): `expCapsule`/`expCapsuleL` prices raised 4x, from 100/450 to 400/1800 (same 4.5x ratio between the two). Unlike Stat Drives, capsules have no purchase cap and no per-use price scaling, so at the old prices a level-1 partner starting with 150 Bits — or after one early Job Board run — could buy straight into several free levels before ever really exploring. No other change; `gainXpCore` and the 250/1,500 XP amounts are untouched.
- Data Mine gear rework (v25.28): `GEAR_ITEMS` grew from 7 flat-bonus items to 17 across three `tier`s (`common`/`rare`/`legendary`) — the original 7 stay Common as-is except Thermal/Frost Core (the two move-granters) moved up to Rare, joined by 5 new Rare items and 4 new Legendary items introducing mechanics the game didn't have before: `resist:{type,pct}` (Type Wards — Insulated Housing/Coolant Jacket/Grounding Strap), `dodgeBonus`/`fleeBonus` (Reflex Coil/Sprint Frame/Vortex Engine — same fields Personality's Focused already uses, just summed from a different source now), `stab` (Resonance Matrix — bonus damage when a move's type matches the partner's own type), and `hpThreshold:{below,mult}` (Last Bastion — DEF spikes while HP is under a ratio). `gearTierWeights(depth)` returns `{common,rare,legendary}` odds that shift with dive depth — rare ramps from depth 1 (caps at 45% around depth ~22), legendary is exactly 0 below `GEAR_LEGENDARY_FLOOR=25` then ramps to a 12% ceiling — WITHOUT changing the drop chance itself (still flat 4% of mine cells, from the pre-existing `['gearDrop', 0.04]` weight in the cell-role table); `rollGearId(depth)` rolls a tier via that distribution then a uniform item within it. The three live-battle mechanics are deliberately NOT folded into `recalcStats` (unlike stat-bonus gear, which already was) because `hpThreshold` needs to react to HP changing turn to turn, not a one-time snapshot: new helpers `gearDodgeBonus`/`gearFleeBonus`/`gearStabBonus`/`gearResistMult(m, moveType)`/`gearHpThresholdDefMult(m)` (all in terms of `equippedGearDefs(m)`) are called directly from `dodgeChance`/`fleeChance`/`battleAction`'s outgoing-damage calc/`enemyTurn`'s incoming-damage calc, computed fresh each time, never written back to `m.def` etc. Sell pricing split: the original 5 Common items keep their exact old individual `GEAR_SELL` prices (nobody's existing stock changes value); everything newer sells by `GEAR_TIER_SELL` (common 80/rare 180/legendary 500) via `gearSellPrice()`'s fallback. `gearEffectText()` grew branches for the four new mechanic shapes. No new save fields — `state.inventory.gear`/`state.gearSeen` already index by arbitrary gear id strings, so the 10 new ids just slot in; `migrateState`'s existing `Object.keys(GEAR_ITEMS).forEach(...)` backfill loop already covers them for free.
- Stat Drive rework (v25.27): converted from "buy generic stock in the Shop, apply later via Inventory's Use button, flat price, cap 10 uses" to the exact same "bought directly for the active partner, applies instantly, price scales" pattern Evolution Crest already used — done because a new player could drop under 1000 Bits (5× flat-200 STR Drives) for a real early boost. `DRIVE_CAP_USES = 5` (was 10 — SUPPLY_ITEMS' `cap` field for the 4 drives is now `amount*DRIVE_CAP_USES`, i.e. the total bonus ceiling, not a use-count). `driveNextPrice(basePrice, uses) = round(basePrice * 2^uses)` — doubles per use already taken, tracked per stat per monster via new `m.driveUses:{hp,str,def,spd}` (separate from `m.driveBonus`, the applied total, so the price curve doesn't have to divide by `amount` to recover a use-count). `buyShopItem()` grew an early `if(def.drive)` branch (before the generic `state.bits < def.price` guard, which would've been wrong here since the live price can be well above the static base) that computes the live price, checks/increments `driveUses`, applies `driveBonus`, and returns — mirrors the existing `evoCrest` special-case immediately below it. `SUPPLY_ITEMS`' 4 drive entries lost `group`/`usable` (they're never stocked in `state.inventory` anymore, so the Inventory screen's Growth section naturally stops listing them — nothing else needed removing there); `useItem()`'s now-dead `SUPPLY_ITEMS[id].drive` stock-consumption branch was deleted outright rather than left unreachable. `renderShop()`'s generic tab renderer shows the live next price and a "`x/5 used`" progress line (same spot Crest's "`x/3 used`" already lived) instead of the static base price. `migrateMonster()` backfills `driveUses` for old saves by inferring it from existing `driveBonus ÷ amount` (clamped to the new cap) — an old save that maxed out under the OLD higher cap is grandfathered (its bonus is never shrunk) but correctly shows as already capped, so it can't buy past the new limit.
- Playtest fixes batch (v25.26), from a fresh-save morning playtest: `INVENTORY_INFO` reordered (Databite/Medicine — the two with a live Use button on that screen — now lead; capture programs and Evolution Crest, never actionable there, moved after). `#screen`'s CSS cap raised from `height:56vh; max-height:400px` to `64vh/560px` — the hard 400px ceiling was leaving most of a tall phone's viewport unused regardless of `vh`; `.wild-screen` (map/mine) is unaffected, it already overrides to `height:auto`. New `recycleDestinationText(type)` — reverse-looks-up which biome a type's boss maps to via `bossTypeOf`, returns null if that type doesn't currently gate anything (Ghost Server's type gates nothing further, or the relevant biome is already unlocked), otherwise a line like "Counts toward unlocking Sunbaked Wastes: Fire-type progress 3/10" shown under the Recycle Entity button on `monsterCard` — previously the only feedback was the log line after the fact. New `evolveHintText(m, nextReq)`: a monster only actually evolves on its NEXT level-up (`checkEvolution` runs inside `gainXpCore`'s level-up loop, never on creation), so a wild monster caught already past its evolution level sat at its current stage showing a stale "Evolves at Lv10" — now, once `m.level >= nextReq - 1` (one level below OR already at/past the requirement — same condition covers both the reported bug and the "show 'next level-up' the level before" ask), it shows "Evolves next level-up" instead of the exact number; replaces the inline ternary at both call sites (Inspect screen, `monsterCard`). Server Farm Grow-Pods are now paginated: `farmPodPage` (transient module var, not persisted, clamped to range every render), 6 pods/page, Prev/Next buttons only shown when `pods.length > 6`; automation (`harvestAll`/`plantAllPods`/`runAutopilot`) still iterates `state.farm.pods` directly and is unaffected by the page slice, which only exists inside `renderFarm`'s render pass.
- Biome unlock, take 2 — Recycle Entity + Network Access Key (v25.25): unlocking biome i+1 now needs THREE conditions in biome i, completable in any order (was: boss defeat alone). `bossTypeOf(biome)` returns `EVOLUTION_LINES[biome.bossSpecies][0].type` — always one of the 8 original roamer types, so it's guaranteed catchable (and thus recyclable) before that biome is ever unlocked, unlike a biome's own favored/habitat list (Foundry/Ghost Server have `favored:[]`, which is why the requirement is NOT "the target biome's favored type"). `biomeUnlockStatus(i)` reads the three conditions live off `state`; `checkBiomeUnlocks()` walks every not-yet-unlocked biome and does `state.unlockedBiomeIndex = Math.max(..., i+1)` (never decreases — safe to call speculatively) when all three are true, logging a 🌐 line only the moment it actually flips. Called from the boss-win branch (after setting `bossDefeated`), from `recycleMonster()`, and from `finishBreach()`'s key grant — never from `migrateState()`, because `checkBiomeUnlocks`/`biomeUnlockStatus` read the closure-scoped `state` variable, which isn't assigned to the object being migrated yet at that point (migration handles its own grandfather case inline on `s` instead, see below). (1) **Recycle Entity**: `recycleMonster(uid)` — a new button on the Network Registry card, right under Sell, sharing Sell's exact eligibility (`canSell`/`sellBlockReason` reused as-is: no active partner, no last monster, no locked monster) and its confirm-tap-then-confirm-button UX pattern (`recycleConfirmUid` mirrors `sellConfirmUid`). Pays **no Bits** (that's what Sell is for) — increments `state.recycleCounts[type]` by 1, a lifetime counter keyed by the 8 `TYPE_IDS`, deliberately never reset or consumed by the unlock check (per an explicit ask to leave room for future reward tiers off the same counter). `RECYCLE_QUOTA = 10`. (2) **Network Access Key**: `finishBreach()` sets `state.biomeKeyFound[biomeIndex] = true` and logs a 🔑 line the first time (only) a Data Breach is solved in that biome; later solves in the same biome are unaffected (still just Bits + capture program). (3) **UI**: Biome Select shows a `✓`/`○` checklist (boss / recycle progress `x/10` / key) for whichever locked biome is immediately next (`i === state.unlockedBiomeIndex + 1`); biomes further out still show the old one-line "defeat the previous boss" placeholder, since showing a full checklist for something 3+ biomes away isn't useful yet. New save fields `state.recycleCounts` (object, all 8 types default 0) and `state.biomeKeyFound` (bool array, `BIOMES.length`) in `defaultState()`; `migrateState()` back-fills both AND detects a pre-feature save via `!Array.isArray(s.biomeKeyFound)` — if so, it grandfathers `unlockedBiomeIndex` forward using the OLD boss-only rule (exactly the pre-existing migration line, now conditional) so nobody who already had a biome open loses access; a save that already has `biomeKeyFound` skips that grandfather branch entirely and relies on the real gameplay call sites to advance it from here on.
- Databite rename (v25.23, same release as the popup-advice fix): "Prime Meat" is now "Databite" everywhere it's displayed (Shop, Inventory, feed messages, Help, README). Purely a display-string swap — the internal inventory key stays `primeMeat` (still referenced that way in `SHOP_ITEMS`, `SUPPLY_ITEMS`, `feedPrimeMeat`-style call sites, `Byte Berry`'s `yieldItem`, etc.), so no migration was needed and no other item/crop logic changed.
- Hunger popup + red glow instead of shake (v25.22): the shake tried in v25.21 (`@keyframes bar-shake`, a few px of translateX) turned out too subtle to notice at the status bar's 8px height, per direct feedback. Replaced with `@keyframes bar-pulse` — a pulsing `box-shadow`/border glow in crimson — and the bar's own fill color now switches to `var(--crimson)` outright while low (previously kept its normal amber/cyan color and only shook). `renderStatusbar()`'s HUNGER/ENERGY label `<div>` also gets a `label-low` class (`color:var(--crimson) !important`) when low, so the word itself goes red, not just the bar. Separately, `moveWild()`'s hunger-depletion branch now calls `showAlertModal(...)` (the existing in-app modal helper, reused as-is — no new modal plumbing) right after `render()`, on top of the `logMsg()` line that already existed — so there's a blocking popup explaining what happened, not just a log line easy to miss during active exploring.
- Low-resource warning + clearer hunger-eject message (v25.21): `renderStatusbar()`'s local `bar(val, color, low)` helper grew a third param that adds class `bar-low` to the `.barwrap` when true; Hunger and Energy pass `p.hunger > 0 && p.hunger <= 20` / same for energy (excludes exactly 0 so it doesn't keep shaking once you're already back in town waiting to feed — the ejection message already covers that moment). `.barwrap.bar-low` is a pure-CSS `@keyframes bar-shake` (small alternating translateX), automatically killed under Reduced Motion by the existing blanket `.motion-reduced *{ animation:none !important; }` rule — no JS motion-check needed. `moveWild()`'s hunger-depletion message (the ONLY thing that currently ejects you mid-exploration — Energy hitting 0 has no effect beyond blocking Jobs/Training/Play, left that way per explicit decision) now explicitly says "You head back to Neon Haven," matching the wording style the faint message already used. The Data Mine's separate hunger-out check (`mineAction`) blocks digging in place rather than ejecting and was intentionally left alone — different design (resumable dives, explicit Disconnect/Abandon), not the "ejected to town" case this was about.
- Field Barriers (v25.20): new map role `'barrier'` with a `reqType` (one of the 8 `TYPE_IDS` = `Object.keys(TYPE_MOVES)`), placed by `generateWildMap()` in its own derived-PRNG block (`mapSeed ^ 0x7f4a7c15`) drawn 2-4 times per map, same additive convert-an-existing-wall-tile pattern as Restricted Zone (never claims open ground, so a favorited seed without the right roster sees no new blockage, only new optional crossings). Unlike Route 1's tools, this is roster-gated, not Shop-gated: `moveWild()` checks `getForm(getActive()).type` against `cell.reqType` — a mismatch blocks with a message naming the required type (no state change, retryable once you swap partners); a match sets `cell.opened=true` and pays the same `25 + biomeIndex*12` Bits + 1 `basicCapture` as Restricted Zone, falling through to normal movement so you step onto the now-open tile in the same action. `TYPE_BARRIER_GLYPHS` gives each type its own map-tile glyph/color (🔥💧🪨🔋🍃🕳🔩👾) — deliberately not reusing any glyph already in `renderWild`'s tile switch, even where the same emoji exists as an item icon elsewhere (different UI context, e.g. 🔥 is also the Thermal Core gear icon). No new save fields, no `migrateWildCell` change needed (an old save simply has no `'barrier'` cells; new maps generate them going forward).
- Personality (v25.19): `PERSONALITIES` (8 entries, keyed by id) and `PERSONALITY_IDS`; each has a `statMult:{hp,str,def,spd}` (defaults to all-1 for the two non-combat ones) and optionally `dodgeBonus`/`fleeBonus`/`hungerEnergyMult`. `m.personality` is rolled once in `newMonster()` via `rollPersonality(rng)` (defaults to `Math.random`, optionally seeded) — same "hidden depth rolled at creation" treatment as IVs. `personalityStatMult(m)` is applied in `recalcStats()` as a fifth multiplier layer, alongside Gear/Crest/Training/Stat Drive (flat bonuses) and Modules (`moduleStatMult`, the other multiplier layer) — `m.maxHp` also gained a multiplier wrapper it never had before (`pm.hp`, defaults to 1, so no behavior change for anyone without a personality bonus on HP). `dodgeChance(defenderSpd, attackerSpd, defenderMon)` grew an optional third param (only the player's own `enemyTurn()` call site passes it — enemies never dodge); `fleeChance(p, enemy)` reads the bonus directly off `p`. Adaptive's Hunger/Energy discount is applied as a multiplier on the existing per-step drain in `moveWild()` — deliberately left un-rounded (hunger/energy are already only ever shown as bar-width percentages, never an exact number in the UI) since the base costs are tiny integers (1-2) that a 15% cut would otherwise round away to nothing. Determinism: `startAnomalyEncounter` rolls personality from the tile's own seeded `rng` (appended AFTER the existing lineId/ivs/marked draws, never inserted before them, so no existing anomaly's identity shifts); `addToWanted`/`startWantedEncounter` carry `entry.personality` through a recapture the same way `marked` already was; `doBreed` does a `Math.random()<0.5` coin flip between the two parents' personalities, then an explicit `recalcStats(child, true)` (newMonster's own internal random roll gets overwritten before anyone reads it). `migrateMonster` backfills a random roll for any monster missing/holding an invalid personality id and forces one `recalcStats` pass so old saves' stats reflect it immediately. Shown on the Inspect screen (new card, name + flavor + `personalityEffectText()` summary) and as a one-line tag on the Network Registry card (kept on its own line below Potential, not inlined into the name row, after the earlier lock-badge crowding regression) — deliberately NOT shown anywhere before capture (encounter/battle screens), matching how IVs are already hidden until Inspect.
- Faint XP penalty (v25.18): in `enemyTurn()`'s faint branch, `p.xp = Math.max(0, p.xp - Math.round(p.xp * 0.1))` before the existing 20%-HP retreat. Since `p.xp` is already just progress within the CURRENT level (`gainXpCore` subtracts `xpToNext(level)` on every level-up and only ever increments `level`), this can only ever set back progress toward the next level — it cannot drop a level already earned, and needed no new save field or migration.
- Layer 2 / Route 2 (v25.17): each biome gets an optional harder remix, unlocked from a new sub-card on its Wilderness Gate entry once `state.bossDefeated[i]` is true (separate from the main biome-unlock chain — beating a Layer 2 boss does NOT unlock the next biome). `BIOMES[i].layer2` is a partial-override object (`levelOffset`, `bossLevel`/`bossLevelOffset`/`bossMult`/`bossTitle`, `desc`, and for Foundry/Ghost Server a denser `hazard`/`fadeFog`) merged over the base biome by `layerBiomeDef(biome, layer)` — `Object.assign({}, biome, biome.layer2)`, so layer 1 (or a biome with no `layer2`) returns the untouched base object. Every call site that used to do `BIOMES[w.biomeIndex]` or `BIOMES[biomeIndex]` for gameplay (not the Gate's own listing) now goes through `layerBiomeDef(..., w.layer||1)` instead, so the SAME boss species is just fought tougher — no new species/sprites needed (bossSpecies is never overridden). `generateWildMap(biomeIndex, sizeOverride, seed, opts)` takes `opts.layer===2`; the actual grid uses `mapSeed = layer===2 ? (useSeed ^ 0x5bd1e995)>>>0 : useSeed` (every other derived-PRNG placement in that function switched from `useSeed ^ const` to `mapSeed ^ const` to match), while the RETURNED `seed` stays the original `useSeed` so the same entered/favorited seed number favorites both layers at once — Layer 2 is never literally the same grid as Layer 1 on that seed, just derived from it. Boss-tile placement checks `state.layer2Defeated[biomeIndex]` instead of `state.bossDefeated[biomeIndex]` when `layer===2`. New Layer-2-exclusive hazard tile `role:'surge'` (⚡, flat ~9% max-HP damage, never drops below 1 HP, no type interaction) is sprinkled at 7% density on `'open'` cells, only when `layer===2`, via its own derived PRNG placed last. `encounterPool(biome, layer)` grew an optional second param that only ADDS a third copy of the favored list when `layer===2` (default/omitted param reproduces the exact legacy `fav+fav+roamers` shape byte-for-byte, so `anomalyPool`'s reuse of it for non-`anomalyIncludesHabitat` biomes is untouched). `anomalySeedRng(mapSeed, x, y, layer)` appends `'_l2'` to the hash key so a Layer 2 anomaly at the same tile coordinates as a Layer 1 one doesn't roll identical species/IVs. `state.wild.layer` (1 or 2) and `state.battle.layer` (set by `startBossEncounter(biomeIndex, layer)`) thread the value through; the boss-defeat branch in the battle-HP-check picks `state.layer2Defeated[b.biomeIndex]=true` (bigger Bits/XP payout, no biome-unlock message) vs. the original `state.bossDefeated[...]=true` + next-biome-unlock path based on `b.layer===2`. New save field `state.layer2Defeated` (array, `defaultState()` + `migrateState()`, same length-sync pattern as `bossDefeated`); `migrateState` also backfills `s.wild.layer=1` for any saved map from before Layer 2 existed.
- Explorer Tools Route 1 (v25.16): three new one-time tools alongside `decrypter` in `EXPLORER_TOOLS`/`state.tools` — `packetTracer` (◈300), `portScanner` (◈350), `firewallBreaker` (◈900). `bossSignalText(w, biome)` (was Ghost-Server-only, took just `w`) now takes `biome` too and returns a generic "🧭 Boss signal: ..." line for any biome when `hasTool('packetTracer')` (or the Ghost Server's own `bossSignal` flavor still wins there, showing "👁 Ghost signal: ..."). Port Scanner: in `renderWild`'s tile loop, a tile is drawn if `w.fog[y][x] || (c.role==='locked' && !c.opened && hasTool('portScanner'))` — reveals unopened Locked Nodes through fog without changing `w.fog` itself (confirmed via DOM inspection: the 🧩 glyph renders at a manually fog-hidden cell once the tool is owned). New map role `'restricted'` (Restricted Zone, ⛔): `generateWildMap()` converts one existing `blockA`/`blockB` wall tile (35% chance, its own derived PRNG `useSeed ^ 0x3c6ef372`, placed after all other placement) into `{role:'restricted', opened:false}` — this only ever repurposes an already-impassable cell, so a favorited seed without the tool sees no difference, and one with the tool only ever gains a new option, never new blockage. `moveWild()` blocks entry without `firewallBreaker` (logs a message, no state change); with it, the first entry sets `opened:true`, pays `25 + biomeIndex*12` Bits + 1 `basicCapture`, and falls through to normal movement so the player steps onto the now-open tile in the same action — re-entry after that is a no-op reward-wise since `opened` is already true. Help hub (`HELP_SECTIONS`, explore topic) got two new cards for these. `defaultState()`'s `tools` object grew the three new keys (all `false`); no `migrateState` change needed since `hasTool()` already tolerates a missing key.
- Auditor/boss-difficulty connective tissue (no version bump yet): text-only addition, no new state/tables/UI, from a content patch supplied for implementation. Makes the (previously coincidental-feeling) fact that bosses get harder each biome while Trace/Auditor escalates read as connected, without ever saying so outright — same self-discovery pattern as the rest of the story layer. Two existing tables got sentences appended: `BIOME_STORY_TEXT`'s neutral `tell` line and all 4 origin reactions for biomes 2-6 (Sunbaked Wastes/Frostbyte Tundra/Undergrid/Overclocked Foundry/Ghost Server — Neon Outskirts deliberately untouched, there's no prior boss yet to contrast against) each gained one more sentence in the "containment/reinforcement" vein (e.g. Insider at Foundry: "...They're not adapting to survive. They're being armed."); `auditorNoticeText()`'s stages 1-5 each gained one bureaucratic-sounding clause ("Standard containment protocols are active at the source." through "Full countermeasure deployment is authorized.") — stage 6 (Ghost Server target-lock) was explicitly left untouched, it's the existing payoff beat. The Ghost Server's `origin` field went from `null` to a real 4-way object as part of this (previously deliberately universal/no-reaction for the final biome; the patch asked for an Insider-tagged line there too, so all 4 origins now got one). No code paths changed — `maybeShowBiomeStoryText()` and `fireAuditorStage()` read these tables exactly as before, so the Archive/diary picks the new text up automatically, same as the design already guaranteed. Verified in-browser: fetched the served file and confirmed all 5 new biome tell-line sentences, the Ghost Server's new Insider line, and both auditor stage 1/5 new clauses are present, while stage 6's text is byte-identical to before; zero console errors on load.
- 5-star Potential rebalance (no version bump yet): direct instruction, "balance it, but not too much." Two small, independent tweaks, both confirmed via a Monte Carlo simulation run in-browser against the exact formulas (`ivPotential`/`potentialStars`/`inheritIvs`) rather than trusted from math alone. (1) `potentialStars()` used to derive its star count via a plain `round(pct*5)`, which as a side effect made the 5-star band exactly HALF the width of every other band (pct>=0.9, a 10%-wide slice of the IV range, vs. 20% for 1-4 stars) — not a deliberate design choice, just what falls out of rounding. Rewritten as explicit bands, unchanged for 1-4 stars, with the 5-star cutoff widened from `pct>=0.9` to `pct>=0.85` (a 15%-wide slice). Wild-capture 5-star odds went from ~1-in-938 to ~1-in-178 in simulation. (2) `inheritIvs()`'s per-stat splice mutation nudge widened from `±0.03` to `±0.05` — only affects Splice Lab children, never wild/anomaly/wanted/boss capture, and does nothing for a mediocre parent pair (still averages out mediocre); two 4-star parents now land a 5-star child ~1-in-32 in simulation, a real payoff for investing in strong breeding stock without being a guarantee. Both numbers were deliberately chosen to keep 5-star clearly the rarest tier (splicing two max-Potential parents is still nowhere near a coin flip) while ending the "never seen one across normal play or testing" experience the old math produced.
- Spliced Variant, a splicing-exclusive cosmetic mutation (no version bump yet): direct instruction — colors that only appear from splicing, plus an "additional mutation" more common than the existing rare one. `SPLICE_VARIANT_CHANCE = 1/10`, rolled ONLY inside `doBreed()` (`child.spliceVariant = Math.random() < SPLICE_VARIANT_CHANCE`, right after `newMonster()` creates the child) — deliberately never inside `newMonster()` itself, unlike Marked (`MARKED_CHANCE = 1/128`, rolled on every new monster including wild/starter/anomaly/wanted), so this stays a genuine splicing-only reward, not just a rarer flavor of an existing universal roll. Three fixed prismatic palettes (`SPLICE_VARIANT_PALETTES` — Prism Shard/Deep Current/Static Bloom), one picked per individual off the existing per-uid `indivRand` seed (stable across renders/reloads, same pattern IV-based lightness and the status-light pixel already used) — a Spliced Variant does NOT use the normal per-individual hue-shift off the species' own color (that's what makes it read as a distinct "mutation" rather than an unusually-saturated regular roll); Marked still wins outright if a spliced child also happens to roll Marked (checked first everywhere: `!m.marked && m.spliceVariant`). `spriteDataUrlRaw()` swaps in the palette's primary/secondary/accent/core-light colors and gives it its own single-pixel tell (bottom-right corner, palette's accent color) distinct from Marked's two white sparkle pixels; `spriteCache`'s key grew `m.spliceVariant?1:0` so it doesn't serve a stale sprite. `spriteImgHtml()`'s glow treats it the same "strong glow" tier as Marked (8px vs. 5px blur) using the palette's own magenta rather than the species color. New per-monster save field `spliceVariant` (bool, default `false` in both `newMonster()` and `migrateMonster()`'s backfill, same shape as `marked`). Badge (`❖ SPLICED` / `❖ Spliced Variant`) added next to the existing Marked badge on both the Network Registry card and Inspect screen; `doBreed()`'s log line calls it out by name. Help hub's Splice Lab card updated to mention both this and the existing IV mutation nudge. Verified in-browser end to end: forced `Math.random` to guarantee the roll, spliced two monsters, confirmed the child's sprite rendered the Prism Shard palette (not the parent species' normal green), the ❖ Spliced Variant badge appeared on both Storage and Inspect, and zero console errors throughout.
- Not yet run: the headless suites against v25 (the harness is not on this machine). Browser smoke tests at 375px passed.

## Invariants (don't break)
- Seeded layouts: don't change RNG call order or pool array length/order for biomes 0-3. Extra features use separate derived PRNGs (`useSeed ^ constant`) placed after base generation.
- Species IDs are append-only.
- Legacy sprites stay byte-identical (except Ferrocog, which got `parts` on purpose).
- New save fields need defaults in both `defaultState()` and `migrateState()`.

## Verification method
Headless jsdom + `node-canvas` harness. Copy the game, inject a hook before the final `init();\n})();` anchor exposing closure functions (`window.__t = {...}`). Never ship the hook. Control time with `clockFn`; patch `window.setTimeout` for synchronous encounter flows. For layout, use the in-app browser at 375x812.

## Open items
- Weekly challenges / event weeks.
- Personality traits, day/night cycle, static town map.
- Mine bosses.
- Balance: Sync Link, gear drop rate, biome level offsets (+6/+8) for a level 50 partner.
- Never checked on a real phone with a full save: long Inventory, the sort bar, Daily screen. Open legend and boss-hint lines on the wilderness map are also unchecked.
